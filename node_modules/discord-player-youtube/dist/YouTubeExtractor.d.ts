import { BaseExtractor, GuildQueueHistory, Track } from "discord-player";
import type { ExtractorInfo, ExtractorSearchContext, ExtractorStreamable, SearchQueryType } from "discord-player";
import type { Types } from "youtubei.js/agnostic";
import type { SabrPlaybackOptions } from "googlevideo/sabr-stream";
export interface ExtractorInitOptions {
    cookie?: string;
    priority?: number;
    filterAutoplayTracks?: boolean;
    innertubeConfigRaw?: Types.InnerTubeConfig;
    sabrPlaybackOptions?: SabrPlaybackOptions;
}
export declare class YoutubeExtractor extends BaseExtractor<ExtractorInitOptions> {
    static identifier: string;
    priority: number;
    private innertube;
    private _stream;
    activate(): Promise<void>;
    deactivate(): Promise<void>;
    validate(query: string, type?: SearchQueryType | null): Promise<boolean>;
    handle(query: string, context: ExtractorSearchContext): Promise<ExtractorInfo>;
    stream(track: Track): Promise<ExtractorStreamable>;
    getRelatedTracks(track: Track, history: GuildQueueHistory): Promise<ExtractorInfo>;
    bridge(track: Track, _sourceExtractor: BaseExtractor | null): Promise<ExtractorStreamable | null>;
}
//# sourceMappingURL=YouTubeExtractor.d.ts.map